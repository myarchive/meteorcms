/* ACL Model
 * This will return whether a user has access to an item
 * Every content and such in the database needs a resource so that access is contained on server side
 */

// To Do: discuss placing which parts in the server side